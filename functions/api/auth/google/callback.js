import {cookie,origin,readCookie} from '../../../_lib.js';
export async function onRequestGet({request,env}){const incoming=new URL(request.url);if(incoming.searchParams.get('state')!==readCookie(request,'cshop_oauth_state'))return new Response('Connexion refusée : vérification de sécurité invalide.',{status:400});const body=new URLSearchParams({code:incoming.searchParams.get('code')||'',client_id:env.GOOGLE_CLIENT_ID,client_secret:env.GOOGLE_CLIENT_SECRET,redirect_uri:origin(request)+'/api/auth/google/callback',grant_type:'authorization_code'});const token=await fetch('https://oauth2.googleapis.com/token',{method:'POST',body}).then(r=>r.json());if(!token.access_token)return new Response('Connexion Google impossible.',{status:400});const profile=await fetch('https://openidconnect.googleapis.com/v1/userinfo',{headers:{authorization:`Bearer ${token.access_token}`}}).then(r=>r.json());if(!profile.email_verified)return new Response('E-mail Google non vérifié.',{status:403});const value=btoa(JSON.stringify({email:profile.email,name:profile.name||profile.email}));return new Response(null,{status:302,headers:{location:'/', 'set-cookie':cookie('cshop_session',value,60*60*24*14)}})}
